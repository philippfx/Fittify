@*@using Fittify.Entities.Sport
@model Workout

@{
    ViewBag.Title = "Workout";
}

<div>
    <h3>@Model.Name, @Model.Id</h3>
    <span style="float: right "><a href="#" class="btn btn-primary">Save</a></span>
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>
                    Exercise
                </th>
                <th>
                    Set 1
                </th>
                <th>
                    Set 2
                </th>
                <th>
                    Set 3a
                </th>
            </tr>
        </thead>
        @foreach (var exercise in Model.Exercises)
        {
            <tr>
                <td>
                    <div>@exercise.Name</div>
                    <div><a href="#" class="btn btn-warning btn-xs ">Delete</a></div>
                </td>
                @foreach (var exerciseHistory in exercise.ExerciseHistories)
                {
                    foreach (var set in exerciseHistory.WeightLiftingSets)
                    {

                        if (set != null)
                        {
                            <td>
                                <div>
                                    <table class="table table-hover" style="text-align: center; width: 150px; margin-bottom: 0">
                                        <thead><tr>
                                            <th colspan="3">Full Weight</th>
                                            <th colspan="3">Reduced</th>
                                            <th>Burn</th>
                                        </tr></thead>
                                        <tr>
                                            <td>
                                                @if (set.RepetitionsFull != null)
                                                {@set.RepetitionsFull}
                                            </td>
                                            <td>x</td>
                                            <td style="border-right: 1px solid">
                                                @if (set.WeightFull != null)
                                                {@set.WeightFull}
                                            </td>
                                            <td>
                                                @if (set.RepetitionsReduced != null)
                                                {@set.RepetitionsReduced}
                                            </td>
                                            <td>x</td>
                                            <td style="border-right: 1px solid">
                                                @if (set.WeightReduced != null)
                                                {@set.WeightReduced}
                                            </td>
                                            <td>@if (set.WeightBurn != null)
                                            {@set.WeightBurn}
                                            </td>
                                        </tr>
                                        <tr>
                                            <td><input type="text" style="width: 30px"/></td>
                                            <td>x</td>
                                            <td style="border-right: 1px solid"><input type="text" style="width: 30px"/></td>
                                            <td><input type="text" style="width: 30px" /></td>
                                            <td>x</td>
                                            <td style="border-right: 1px solid"><input type="text" style="width: 30px" /></td>
                                            <td><input type="text" style="width: 30px" /></td>
                                        </tr>
                                    </table>


                                </div>
                            </td>
                        }

                    }
                }
            </tr>
        }
    </table>
    <div style="float: left "><a href="#" class="btn btn-primary">Add</a></div>
    <div style="float: right "><a href="#" class="btn btn-primary">Save</a></div>
</div>*@